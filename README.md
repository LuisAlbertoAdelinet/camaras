# Android app (Kotlin + Compose)

## Setup
1) Open `android/` in Android Studio.
2) Update `API_BASE_URL` in `android/app/build.gradle.kts`.
3) Sync Gradle and run on device.

## Notes
- RTSP playback uses Media3.
- Snapshot and recording buttons are placeholders for the next step.
